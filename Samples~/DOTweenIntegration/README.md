# DOTween Integration

Prerequisites:

1. Install and set up DOTween in the host project.
2. Add `COZY_GIBBERISH_DOTWEEN` to **Project Settings > Player > Scripting Define Symbols**.

`GibberishTalkMotion` listens to syllable events and produces a soft scale pulse. It contains no polling, coroutine or manual animation loop.
