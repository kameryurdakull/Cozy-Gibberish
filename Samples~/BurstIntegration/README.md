# Burst Integration

Install Burst, Collections and Mathematics, then add `COZY_GIBBERISH_BURST`.

Register `BurstGibberishSynthesizer` through VContainer to use a `NativeArray`/Jobs/Burst post-processing backend. The core renderer remains the compatibility fallback.
