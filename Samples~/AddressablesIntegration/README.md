# Addressables Integration

Install Addressables and add `COZY_GIBBERISH_ADDRESSABLES`. The loader keeps voice/style assets outside the initial scene and releases their handles with the owning object.
