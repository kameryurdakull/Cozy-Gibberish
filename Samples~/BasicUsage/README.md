# Basic Usage

1. Open `Tools > Cozy Gibberish > Setup Wizard`.
2. Assign the generated player, voice and style assets to `GibberishBasicExample`.
3. Enter Play Mode and invoke either component context-menu command.

The example intentionally has no input-system dependency.
