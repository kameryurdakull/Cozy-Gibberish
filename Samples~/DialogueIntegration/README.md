# Dialogue Integration

`GibberishDialogueBridge` exposes dependency-free command methods for Yarn Spinner, Ink and custom dialogue runners.

For Unity Localization, install the package and add `COZY_GIBBERISH_LOCALIZATION`; `GibberishLocalizedStringBridge` then resolves `LocalizedString` values before speaking.
