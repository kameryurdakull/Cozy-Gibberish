# TMP Typewriter Integration

Install TextMeshPro and add `COZY_GIBBERISH_TMP` to Scripting Define Symbols.

`GibberishTmpTypewriter` reveals words against their exact scheduled DSP timestamps. Rich-text tags remain hidden from the visible-character count.
